apiVersion: v1
kind: Service
metadata:
  name: customer-analytics-dashboard
  labels:
    app: customer-analytics-dashboard
spec:
  type: LoadBalancer
  selector:
    app: customer-analytics-dashboard
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  sessionAffinity: None
